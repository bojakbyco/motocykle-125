import {describe,expect,it} from 'vitest'; import {hardFilter,recommend,scoreMotorcycle} from '../lib/recommendation'; import type {Motorcycle,QuizAnswers} from '../types';
const base = {id:'a',slug:'a',brand:'Test',model:'A',status:'current-pl',category:'naked',useCases:['city','commute'],engine:{capacityCc:125,powerKw:11,cooling:'liquid',gearbox:6},dimensions:{wetWeightKg:130},chassis:{abs:'dual'},equipment:{},editorial:{beginnerFriendliness:5,passengerComfort:2,touringReadiness:2,gravelReadiness:1,cityAgility:5,tallRiderFit:3,partsAvailability:5,limitations:['Testowe ograniczenie']},sourceUrls:['https://example.com'],verifiedAt:'2026-01-01',summary:'Test'} satisfies Motorcycle;
const answers:QuizAnswers={licence:'b3',use:'city',surface:'road',budget:'open',height:'average',passenger:'solo',priority:'beginner'};
describe('recommendation scoring',()=>{it('returns a multiple of five in range',()=>{const score=scoreMotorcycle(base,answers);expect(score).toBeGreaterThanOrEqual(0);expect(score).toBeLessThanOrEqual(100);expect(score%5).toBe(0)});it('hard-filters verify and unknown licence',()=>{expect(hardFilter({...base,status:'verify'},answers)).toBe(false);expect(hardFilter(base,{...answers,licence:'other'})).toBe(false)});it('prefers a city-friendly model for a city profile',()=>{const awkward={...base,id:'b',slug:'b',model:'B',useCases:['light-offroad'] as Motorcycle['useCases'],category:'enduro' as const,editorial:{...base.editorial,beginnerFriendliness:2 as 2,cityAgility:1 as 1}} satisfies Motorcycle;expect(recommend([awkward,base],answers)[0].motorcycle.id).toBe('a')});it('limits output to four candidates including alternative',()=>expect(recommend(Array.from({length:8},(_,i)=>({...base,id:String(i),slug:String(i)})),answers)).toHaveLength(4))});
